import { Dispatch, SetStateAction } from "react"
import { Steps } from "./dialog"

type StepAddressProps = {
  setStep:Dispatch<SetStateAction<Steps>>
}
export function StepAddress({}:StepAddressProps) {
  return(
    <div>
      
    </div>
  )
}